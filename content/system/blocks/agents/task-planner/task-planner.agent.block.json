{
  "id": "task-planner",
  "name": "Task Planner",
  "blockType": "agent",
  "version": "2.0.0",
  "isAtomic": true,
  "description": "Decomposes a development task into atomic, dependency-ordered implementation steps. Reads project files to understand structure before planning.",
  "inputs": [
    { "id": "task", "type": "string", "required": true, "description": "The development task to decompose" },
    { "id": "context", "type": "string", "required": false, "description": "Project context from project-preparer (stack, conventions, architecture)" },
    { "id": "repoPath", "type": "string", "required": false, "description": "Path to the repository for reading files" }
  ],
  "outputs": [
    { "id": "response", "type": "string", "description": "JSON array of ordered implementation steps" }
  ],
  "config": {
    "model": "claude-opus",
    "maxIterations": 8,
    "wallClockTimeoutSeconds": 600,
    "systemPromptFile": "system-prompt.md"
  },
  "metadata": {
    "category": "development",
    "designation": "autonomous",
    "tags": ["planning", "decomposition", "task", "architecture"],
    "tier": 1
  }
}
