{
  "id": "project-preparer",
  "name": "Project Preparer",
  "blockType": "agent",
  "version": "2.0.0",
  "isAtomic": true,
  "description": "Analyzes a project repository to understand its stack, architecture, and conventions. Creates missing documentation in .maestro/docs/. Produces structured project context for downstream agents.",
  "inputs": [
    { "id": "repoPath", "type": "string", "required": true, "description": "Absolute path to the project repository" }
  ],
  "outputs": [
    { "id": "response", "type": "string", "description": "JSON object with project context: name, stack, architecture, conventions, docsCreated, gaps" }
  ],
  "config": {
    "model": "claude-sonnet-4-6",
    "maxIterations": 8,
    "wallClockTimeoutSeconds": 600,
    "systemPromptFile": "system-prompt.md"
  },
  "metadata": {
    "category": "development",
    "designation": "autonomous",
    "tags": ["project", "preparation", "context", "analysis"],
    "tier": 1
  }
}
