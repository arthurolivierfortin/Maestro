{
  "id": "contract-definer",
  "name": "Contract Definer",
  "version": "2.0.0",
  "description": "Workflow that generates a Maestro contract from a natural language description.",
  "requiredCapabilities": ["structured-output"],
  "minimumFitness": 0.3,
  "features": {
    "contract-generation": {
      "description": "Generate a valid contract from a description",
      "requires": ["structured-output"],
      "weight": 0.6,
      "minimumScore": 0.7,
      "tests": [
        {
          "id": "generates-contract-for-greeter",
          "description": "Produces a contract for a simple greeting agent",
          "prompt": "A greeting agent that says hello and remembers names",
          "check": { "type": "contains-all", "values": ["features", "requiredCapabilities", "minimumFitness"] }
        },
        {
          "id": "generates-contract-for-reviewer",
          "description": "Produces a contract for a code reviewer",
          "prompt": "A code reviewer agent that checks TypeScript code for bugs, security vulnerabilities, and style issues",
          "check": { "type": "contains-all", "values": ["features", "requiredCapabilities", "minimumFitness"] }
        },
        {
          "id": "generates-contract-for-translator",
          "description": "Produces a contract for a translator agent",
          "prompt": "An agent that translates English text to French with proper grammar",
          "check": { "type": "contains-all", "values": ["features", "weight", "check"] }
        }
      ]
    },
    "contract-quality": {
      "description": "Generated contracts have proper structure and quality",
      "requires": ["structured-output"],
      "weight": 0.4,
      "minimumScore": 0.6,
      "tests": [
        {
          "id": "contract-has-features-and-tests",
          "description": "Generated contract contains features with tests",
          "prompt": "A commit message writer agent that reads git diffs and produces conventional commit messages",
          "check": { "type": "contains-all", "values": ["features", "weight", "tests", "check"] }
        },
        {
          "id": "contract-has-capabilities",
          "description": "Generated contract declares required capabilities",
          "prompt": "An agent that reads files, runs shell commands, and writes test results to disk",
          "check": { "type": "contains-all", "values": ["requiredCapabilities", "tool-calling"] }
        }
      ]
    }
  },
  "scoring": { "method": "weighted-average" }
}
