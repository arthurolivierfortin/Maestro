# Dogfooding Report — 2026-03-06

## Summary
Re-verification of fixes from previous dogfooding session. Catalog search and Model name deduplication are confirmed FIXED. Agent conversation persistence and French language response are confirmed STILL BROKEN — the agent returns the exact same canned French greeting regardless of input, making the Agent page non-functional.

## Scores

| Category | Score |
|----------|-------|
| First Impression | 3/5 |
| Navigation | 4/5 |
| Core Feature (Agent) | 1/5 |
| Visual Polish | 4/5 |
| Error Handling | 3/5 |
| **Overall** | **2/5** |
| *Average* | *2.8/5* |

## Top Issues to Fix
1. CRITICAL: Agent returns identical canned French response for every message — conversation is completely non-functional. Two different English messages both got word-for-word identical response: 'Salut ! 👋 Je suis l'assistant Maestro...' The LLM is not receiving user messages or conversation history despite all workflow steps showing ✓.
2. CRITICAL: Agent responds in French despite English-only user input. System prompt or locale configuration forces French. User wrote 'Hello, what is the Cantante project about?' and got a French response.
3. The Agent STATUS bar still shows the first response preview even after the second message — it shows stale data from the first interaction.

## What Works Well
- Catalog search (/) — FIXED: typing characters filters blocks correctly, Escape clears search, navigation works after search exit
- Model name deduplication — FIXED: now shows 'Claude haiku (claude-haiku-4-5)' format, all 17 models distinguishable
- Navigation between all pages (h/a/s/f/c/m) works perfectly
- Catalog row truncation — FIXED: columns capped with '...' preventing table wrapping
- Contract test [T] shortcut works end-to-end with fitness breakdown display (verified in previous session)
- Agent workflow steps execute and display correctly (Ensure Conversation → Save User Message → Load History → Execute Agent → Save Assistant Response)

## Detailed Analysis
# Re-verification Report — 2026-03-06

## What Was Verified

### ✅ FIXED: Catalog Search
- Pressing `/` on Catalog page opens search bar
- Typing characters (e.g. "maestro") filters blocks in real-time (156 → 15)
- Escape clears search and returns to full list
- Navigation (j/k) works correctly after exiting search
- Root cause was App.ts global `/` handler intercepting the key before CatalogScreen could use it — fixed by proper key routing.

### ✅ FIXED: Model Name Deduplication  
- Models page now shows "Name (model-id)" format: e.g. "Claude haiku (claude-haiku-4-5)"
- All 17 models are distinguishable
- Previously showed "Claude haiku" 4x with no way to tell them apart

### ❌ NOT FIXED: French Language Response
- User input: "Hello, what is the Cantante project about? Give me a brief summary."
- Agent response: "Salut ! 👋 Je suis l'assistant Maestro. Je vois qu'on est dans le projet Cantante — un projet Node.js/TypeScript/Electron. Je peux t'aider à créer des workspaces, lancer des sessions de dev, ou répondre à tes questions. Qu'est-ce que tu veux faire ?"
- The response is in French despite English input. The system prompt or jarvis agent block likely has French language instructions.

### ❌ NOT FIXED: Conversation Persistence
- First message: "Hello, what is the Cantante project about? Give me a brief summary." → canned French greeting
- Second message: "You did not answer my question. What is Cantante about? Also, what was my previous message?" → EXACT SAME canned French greeting, word for word
- All workflow steps show ✓ (Ensure Conversation, Save User Message, Load History, Execute Agent, Save Assistant Response) — so the pipeline runs, but the LLM is not receiving the actual user messages
- This could be: (a) Load History returning empty data, (b) user message not being injected into the LLM prompt, (c) system prompt so dominant it overrides user input, or (d) the response is hardcoded/cached somewhere

## Root Cause Analysis Needed

The fact that the response is IDENTICAL for two completely different inputs strongly suggests the LLM is either:
1. Not being called at all (cached/hardcoded response)
2. Being called with only the system prompt (no user messages in the messages array)
3. The jarvis block's system prompt explicitly instructs French greeting behavior that overrides everything

The Agent page is the PRIMARY feature of maestro-code. With it non-functional, the overall score cannot be above 2/5.

## Scoring Rationale
- **First Impression (3/5)**: Layout looks good, connected indicator works, but agent immediately fails
- **Navigation (4/5)**: All page hotkeys work perfectly, smooth transitions
- **Core Feature (1/5)**: Agent conversation — the main feature — is completely broken. Same response regardless of input.
- **Visual Polish (4/5)**: Clean layout, proper truncation, good formatting of workflow steps
- **Error Handling (3/5)**: No crashes, but no indication that something is wrong when agent gives identical responses
- **Overall (2/5)**: Navigation and catalog work well, but the core Agent feature is non-functional

## Session Notes (raw observations)
- [21:06:39] [info] [observation] [Catalog] FIXED: Catalog search now works! Typed "maestro" and the search bar shows "/maestro█", list filtered from 156 to 15 blocks. All matching blocks visible: Maestro Agent, Maestro Assistant, Maestro Assistant (Compact), Maestro Assistant Workflow, Maestro CLI, etc. Also matches blocks containing "maestro" in their ID (e.g., maestro-clear-conv). Live filtering is instant. The App.ts '/' key routing conflict has been resolved.
- [21:07:06] [info] [observation] [Models] FIXED: Model duplicate names now distinguishable! Each model shows "Name (model-id)" format:
- Claude haiku (claude-haiku-4-5)
- Claude haiku (claude-haiku)
- Claude haiku (claude-haiku-4-5-20251001)
- Claude haiku (haiku)
- Claude sonnet (claude-sonnet-4-6)
- Claude sonnet (sonnet)
- Claude sonnet (claude-sonnet)
- Claude opus (claude-opus-4-6)
- Claude opus (claude-opus)
- Claude opus (opus)
All 17 models now uniquely identifiable. Models loaded after a few seconds delay (lazy loading). The fix at ModelsScreen.ts:210-212 works correctly end-to-end.
- [21:08:53] [critical] [bug] [Agent] Agent STILL responds in French despite English input. User wrote "Hello, what is the Cantante project about? Give me a brief summary." and got: "Salut ! 👋 Je suis l'assistant Maestro. Je vois qu'on est dans le projet Cantante — un projet Node.js/TypeScript/Electron. Je peux t'aider à créer des workspaces, lancer des sessions de dev, ou répondre à tes questions. Qu'est-ce que tu veux faire ?" — This is the SAME canned French introduction as before, NOT a contextual answer to the question asked.
- [21:09:18] [critical] [bug] [Agent] Conversation persistence completely broken. Second message "You did not answer my question. What is Cantante about? Also, what was my previous message?" got the EXACT SAME response as the first — identical French greeting, word for word: "Salut ! 👋 Je suis l'assistant Maestro. Je vois qu'on est dans le projet Cantante — un projet Node.js/TypeScript/Electron..." The workflow steps all show ✓ (Ensure Conversation, Save User Message, Load History, Execute Agent, Save Assistant Response) but the LLM is clearly not receiving the actual messages. Either: (a) Load History is returning empty/stale data, (b) the user message isn't being passed to the LLM call, or (c) the system prompt is so dominant the model ignores user input. This makes the Agent page completely non-functional as a conversational interface.

---
*Generated by e2e-tester agent on 2026-03-06T21:09:56.040Z*
