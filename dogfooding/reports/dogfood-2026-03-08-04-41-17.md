# Dogfooding Report — 2026-03-08

## Summary
Phases 56-58 dogfooding shows strong results. Catalog search, Models page, /help, /create-agent usage, conversation persistence, and block discovery all work well. The main gap is /create-agent execution failing with "Not Found" (block-forge workflow invocation issue). Minor UI bugs: block detail shows Type [unknown], Providers count shows '-' in Models status panel.

## Scores

| Category | Score |
|----------|-------|
| First Impression | 4/5 |
| Navigation | 5/5 |
| Core Feature (Agent) | 4/5 |
| Visual Polish | 4/5 |
| Error Handling | 4/5 |
| **Overall** | **4/5** |
| *Average* | *4.2/5* |

## Top Issues to Fix
1. MAJOR: /create-agent execution fails with 'Error: Not Found' — block-forge workflow session/invoke returns 404. The UI flow (parsing, progress message) works but the backend invocation fails.
2. MINOR: Block detail view shows Type: [unknown] instead of the actual blockType (e.g. 'agent' for test-designer). blockType not passed to detail/expand view.
3. MINOR: MODEL STATUS panel shows Providers: '-' despite providers being visible in PROVIDERS panel. The health DTO doesn't include providers object — providerCount fallback also missing.
4. MINOR: Catalog search Escape clears filter instead of keeping filtered results. Enter confirms but Escape discards — could be confusing.
5. INFO: Block detail view missing capabilities, contract, and tags fields in INFO panel — only shows ID, Type, Atomic, Version, Author.

## What Works Well
- Catalog search — instant filtering, 159→15 blocks for 'maestro', correct results
- Models page — 17 models with IDs in parentheses (Claude Sonnet (claude-sonnet-4-6)), no more duplicates
- /help command — complete list including /create-agent with description
- /create-agent usage — clear usage message with example when called without arguments
- Conversation persistence — agent remembers 'TestUser' across messages, ~8s response time
- Block discovery — block-forge (workflow) and request-analyzer (inference) both found in catalog
- Execution pipeline visibility — checkmarks for each step (Read Conversation, Llm Call, Execute Agent, etc.)
- Navigation — all hotkeys work (h/a/s/f/c/m), page switching instant
- [T] Test hint appears for blocks with contracts

## Detailed Analysis
## Detailed Analysis — Phases 56-58 Dogfooding

### Test 1 — Catalog Search (PASS)
Catalog loads 159 blocks. Layout clean — each block on one line, columns aligned (type badge, name, ID, fitness, tags/description). Hint bar visible at bottom. Search with `/` + `maestro` filters to 15 blocks instantly. All results relevant. Search bar shows `/maestro` with cursor.

### Test 2 — Catalog [T] Contract Test (PASS)
Search for `test-designer` finds 2 results. [T] Test hint appears in hint bar — confirms contract detection works. Block detail view on Enter shows version and atomic on SEPARATE lines (concatenation bug fixed). However, Type shows `[unknown]` instead of `[agent]`, and capabilities/contract/tags are missing from the INFO panel.

### Test 3 — Models Page (PASS)
17 models displayed with IDs in parentheses: `Claude haiku (claude-haiku-4-5)`, `Claude sonnet (claude-sonnet-4-6)`, `Claude opus (claude-opus-4-6)`. No more duplicate "Claude Sonnet" x3. Status panel shows Online status, Models: 4, Device: managed. But Providers shows `-` (health DTO missing providers object). Queue panel shows real data (depth 0). Providers panel correctly lists Claude Code (CLI) and Local (FastAPI) with [R] Reconfigure hint.

### Test 4 — /help (PASS)
Complete command list displayed including `/create-agent — Create an agent via block-forge workflow`. All 9 commands listed: /help, /status, /new, /clear, /stop, /purge, /agent, /create-agent, /quit. Keyboard shortcuts and page navigation also shown.

### Test 5 — /create-agent Usage (PASS)
Without arguments shows: `Usage: /create-agent <description> [--contract <id>] [--model <id>]` with example: `/create-agent An agent that reviews TypeScript code --contract code-reviewer`. Clear and helpful.

### Test 6 — /create-agent Execution (PARTIAL)
With arguments shows "Creating agent via block-forge workflow..." with parsed Description and Contract. Then immediately: `Error: Not Found`. The block-forge workflow invocation via session/invoke returns 404. The UI parsing and progress flow work correctly, but the backend invocation path is broken. Likely the block-forge workflow doesn't have proper entry points or the session creation endpoint for workflow blocks returns 404.

### Test 7 — Conversation Persistence (PASS)
First message: "My name is TestUser" → Agent responds "Nice to meet you, TestUser! I'm the Maestro assistant — your conversational orchestrator."
Second message: "What is my name?" → Agent responds "Your name is TestUser! You told me at the start of our conversation." 
Full conversation persistence confirmed. Execution pipeline visible with checkmarks. ~8s response time per message.

### Test 8 — Block Discovery (PASS)
Search `block-forge` finds: Block Forge Workflow (type: workflow) and Request Analyzer (type: inference). Both Phase 58 blocks correctly discovered and visible in the catalog.

### Summary
7/8 tests PASS, 1 PARTIAL. The core functionality from Phases 56-58 is solid. The /create-agent command infrastructure is in place (parsing, usage, progress UI) but the actual workflow invocation fails — this is the main item to fix. Minor UI issues in block detail view and Models status panel are cosmetic.

## Session Notes (raw observations)
- [04:36:34] [info] [observation] [Catalog] Catalog page loads with 159 blocks. Layout: each block = 1 line, columns aligned (type badge, name, ID, fitness, tags/description). Hint bar visible at bottom: [1-4] Filter, [↑↓] Navigate, [Enter] Expand, [/] Search, [Esc] Back. Block Forge and Agent Creator visible in the list. All looks good.
- [04:36:46] [info] [delight] [Catalog] Search works perfectly. Typing 'maestro' filters from 159 to 15 blocks. Search bar shows '/maestro' with cursor. Results are relevant — all contain 'maestro' in name/id/description. Instant filtering, no lag.
- [04:37:03] [info] [delight] [Catalog] Search 'test-designer' shows 2 results (block-forge workflow which references test-designer, and test-designer itself). The hint bar now shows [T] Test — this appears because both blocks have contracts. Excellent.
- [04:37:18] [minor] [friction] [Catalog] Escape from search mode clears the search filter and restores all 159 blocks, losing the filtered view. Arrow keys don't work while search is active. To navigate filtered results, you'd need to press Enter to confirm search first, then use arrows. This is mildly confusing — pressing Escape should maybe keep the filter but exit input mode.
- [04:37:49] [minor] [observation] [Catalog] Block detail view for test-designer: Shows INFO panel with ID, Type ([unknown] — should be 'agent'), Atomic: yes, Version: 1.0.0, Author: -. FITNESS panel shows 'no fitness data'. SESSIONS panel shows 0 sessions. Version and Atomic are on SEPARATE lines — the concatenation bug is fixed. However Type shows [unknown] instead of [agent] — possible blockType mapping issue in the detail view.
- [04:37:53] [minor] [bug] [Catalog] Test Designer block shows Type: [unknown] in the detail view, but in the list view it correctly shows [agent]. The blockType is not being passed correctly to the detail/expand view. Also, capabilities are NOT shown in the detail view — the INFO panel only shows ID, Type, Atomic, Version, Author. Missing: capabilities, contract, tags.
- [04:38:11] [info] [observation] [Models] Models page loaded with 17 models. Model IDs shown in parentheses for Claude models: 'Claude haiku (claude-haiku-4-5)', 'Claude sonnet (claude-sonnet-4-6)', 'Claude opus (claude-opus-4-6)' — PASS, no more duplicate 'Claude Sonnet' x3. Status panel: Status=Online, Providers='-' (should show count like 4), Models=4, Device=managed. Queue panel shows real data (depth 0). Metrics panel shows 'No metrics available'. Providers panel correctly shows Claude Code (CLI) and Local (FastAPI).
- [04:38:17] [minor] [bug] [Models] Providers count shows '-' in MODEL STATUS panel despite 2 providers being visible in PROVIDERS panel (Claude Code CLI + Local FastAPI). The health endpoint returns providers as an object but the panel reads providerCount which doesn't exist — it should count Object.keys(health.providers).length. The health response likely doesn't include the 'providers' key at the Maestro backend level (it's mapped away in LLMProviderService.GetHealthAsync).
- [04:38:36] [info] [delight] [Agent] /help shows complete command list including '/create-agent — Create an agent via block-forge workflow'. All commands present: /help, /status, /new, /clear, /stop, /purge, /agent, /create-agent, /quit. Keyboard shortcuts and page navigation also documented. Clean layout.
- [04:38:53] [info] [delight] [Agent] /create-agent without arguments shows usage message: 'Usage: /create-agent <description> [--contract <id>] [--model <id>]' with example: '/create-agent An agent that reviews TypeScript code --contract code-reviewer'. Perfect UX — clear usage + concrete example.
- [04:39:12] [major] [observation] [Agent] /create-agent with arguments shows 'Creating agent via block-forge workflow...' with parsed Description and Contract. Then immediately shows 'Error: Not Found'. The block-forge workflow invocation is attempted but the session/invoke endpoint returns 404. This could mean the block-forge workflow isn't properly registered as an invocable entry point, or the session creation for it fails. The error message is clear though — good error handling.
- [04:40:10] [info] [delight] [Agent] Conversation persistence works perfectly. Agent remembers 'TestUser' from previous message. Response: 'Your name is TestUser! You told me at the start of our conversation.' Execution pipeline visible: Read Conversation → Llm Call → Append Assistant → Parse Response → Ensure Conversation → Save User Message → Load History → Execute Agent → Save Assistant Response. ~8 seconds total response time.
- [04:40:32] [info] [delight] [Catalog] Search 'block-forge' finds both: Block Forge Workflow (type: workflow) and Request Analyzer (type: inference). Both blocks from Phase 58 are correctly discovered and visible in the catalog. block-forge has orchestration tags, request-analyzer shows 'Analyzes a natural...' description. [T] Test hint visible — both have contracts.

---
*Generated by e2e-tester agent on 2026-03-08T04:41:17.118Z*
