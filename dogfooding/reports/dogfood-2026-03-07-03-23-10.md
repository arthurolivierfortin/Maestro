# Dogfooding Report — 2026-03-07

## Summary
Comprehensive dogfooding session verifying three specific fixes plus full app re-verification in both demo and real modes. All targeted fixes confirmed working: Catalog '/' search key routing, Models page model listing (17 models in real mode), and Agent conversation persistence. The app is in solid shape — every page loads correctly, navigation is instant, and the core agent workflow functions end-to-end.

## Scores

| Category | Score |
|----------|-------|
| First Impression | 4/5 |
| Navigation | 5/5 |
| Core Feature (Agent) | 4/5 |
| Visual Polish | 4/5 |
| Error Handling | 3/5 |
| **Overall** | **4/5** |
| *Average* | *4.0/5* |

## Top Issues to Fix
1. [VERIFIED FIXED] Catalog '/' key routing: '/' on Catalog enters search mode, on Agent focuses input. Context-sensitive per page.
2. [VERIFIED FIXED] Models page: 17 real models shown from LLM-Provider (was '0 models'). All 4 providers visible (Azure, AzureInference, Anthropic, Local).
3. [VERIFIED FIXED] Agent conversation persistence: session d5ae0fec persisted across TUI restarts. Third message ('2+2') processed correctly after two previous messages from earlier session.
4. [MINOR] Models page in real mode: Active Model shows '-', Backend/MaxTokens/Temperature all '-'. Health endpoint doesn't return these fields. Only device='managed' populated.
5. [MINOR] Catalog expanded block rendering: slight text overlap artifacts when expanded row pushes surrounding rows. 'atomic: no.0.0' concatenation issue (version appended to boolean).

## What Works Well
- All 6 pages load and display correctly in both demo and real modes
- Navigation hotkeys (h/a/s/f/c/m) work instantly across all pages
- Catalog search ('/' key): enters search mode, filters in real-time, Enter confirms filter, Escape clears. Searched 156 real blocks successfully.
- Catalog [T] contract test: auto-expands block, shows fitness breakdown (Performance/Specialization/Composability/Cost Factor), features with progress bars, test results
- Models page: 17 models from 4 providers with categories (cloud/local). Model status, queue stats, provider details all displayed.
- Agent conversation: full workflow (ensure-conversation → save-user-message → load-history → execute-agent → save-assistant-response) works end-to-end
- Session persistence: session.json preserves session ID across TUI restarts, conversation history maintained
- Real-time execution tree updates during agent processing
- 147 sessions visible in Spaces with scroll, entry points, status, duration
- 156 blocks in Catalog/Foundry with type badges, descriptions, capabilities

## Detailed Analysis
# Full Dogfooding Report — 2026-03-07

## Scope
This session verified three specific fixes and performed a full in-depth re-verification of the entire app in both demo and real modes.

### Fix 1: Catalog '/' Search Key Routing
**Status: VERIFIED FIXED**

The fix in App.ts line 362 (`input === '/' && currentPageRef.current === 'agent'`) correctly makes '/' context-sensitive:
- **Agent page**: '/' focuses the TaskInputBar input (shows '> Describe your task...')
- **Catalog page**: '/' enters CatalogScreen's built-in search mode (shows '/█' cursor)
- **Other pages**: '/' passes through without effect

Tested search workflow: typed 'agent' → filtered to 1 result, typed 'maestro' → filtered 156 blocks to 15. Enter confirms filter (shows 'filter: maestro [Esc] clear'), Escape clears. All correct.

**Minor suggestion**: Search filters by name/id/description/capabilities but NOT by blockType. Searching 'agent' only finds blocks with 'agent' in their name, not all agent-type blocks. Consider adding blockType to searchable fields.

### Fix 2: Models Page (0 models → 17 models)
**Status: VERIFIED FIXED**

**Demo mode**: 6 mock models displayed with full panel data (MODEL STATUS: Online, claude-sonnet-4-6; METRICS: 142 requests, 57K tokens; QUEUE: depth 0; PROVIDERS: 2 configured).

**Real mode**: 17 real models from 4 providers:
- Anthropic: Claude haiku/sonnet/opus (multiple aliases)
- Azure: gpt-4o, gpt-4
- AzureInference: mistral-large, phi-3-medium, llama-3-70b, cohere-command-r-plus
- Local: deepseek-coder-1.3b

The HttpClient factory fix in Program.cs is confirmed working — the ModelsScreen successfully calls `listLLMModels()` through the backend proxy to LLM-Provider.

**Minor gaps in real mode**: Active Model shows '-', Backend/MaxTokens/Temperature all '-'. The LLM-Provider health endpoint returns `status: healthy` and `providers: {...}` but doesn't include `activeModel`, `backend`, or `maxTokens` fields. Metrics shows 'No metrics available' (expected on fresh session with no LLM calls from the metrics endpoint).

### Fix 3: Agent Conversation (re-verified)
**Status: VERIFIED FIXED**

The agent conversation now works end-to-end including cross-restart persistence. This TUI session reused session d5ae0fec (from the previous dogfooding), sent a third message ("What is 2+2? Answer in one word."), and got "Four." back in ~14s. The workflow tree showed all steps completing successfully.

## Full App Re-verification

### Navigation (Score: 5/5)
All 6 page hotkeys work instantly: h=Home, a=Agent, s=Spaces, f=Foundry, c=Catalog, m=Models. Ctrl+←→ also available. No lag, no missed keypresses. Back navigation with Escape works correctly (search clear → expanded clear → page back).

### Home Page
- Demo: 5 sessions with status icons (✓/⠹/✗), badge "5 sessions (1 running)"
- Real: "No active sessions" (correct — no running workflows)

### Spaces Page
- Demo: 5 sessions, first auto-expanded, tab filter
- Real: 147 sessions loaded with scroll (1/147 ▼), real names/statuses/durations, entry points visible

### Foundry Page
- Demo: 14 blocks with type breakdown
- Real: 156 blocks, full type breakdown (34 workflow, 44 agent, 39 tool, 18 inference, etc.)

### Catalog Page
- Demo: 14 blocks with fitness scores, capabilities, descriptions. Search and [T] test work.
- Real: 156 blocks loaded, search filters work, fitness scores show '-' (no mock data)

### Models Page
- Demo: All panels populated with realistic mock data. 6 models.
- Real: 17 models, 4 providers. Status/Queue panels populated. Metrics panel empty (expected).

### Agent Page
- Demo: Auto-task "Add login page" starts immediately, execution tree animates
- Real: '/' focuses input, message sent, workflow executes, response displayed with execution tree

## Rendering Issues (Minor)
1. Catalog expanded block: text overlap when expanded row pushes surrounding rows ('Commit Helper block' overlapping with 'r block')
2. 'atomic: no.0.0' — version number concatenated with boolean value without separator
3. These are viewport/terminal rendering artifacts, not logic bugs

## Score Justification
- **First impression (4/5)**: Clean layout, intuitive pages. Status bar informative. Demo mode provides great preview.
- **Navigation (5/5)**: Perfect — all hotkeys work, instant page switching, back navigation correct
- **Core feature (4/5)**: Agent conversation, catalog search, models listing all work. Contract test with breakdown display works. -1 for Models page missing some real-mode data fields.
- **Visual polish (4/5)**: Consistent design across all pages. Type badges, progress bars, status icons. Minor rendering artifacts on catalog expand.
- **Error handling (3/5)**: Models page gracefully shows "Is the LLM provider running?" when models aren't available. But silent Task.Run failures from earlier session remain a concern — background errors need better surfacing.
- **Overall (4/5)**: Solid application with all major features working. The three targeted fixes are confirmed. Ready for continued development.

## Session Notes (raw observations)
- [03:20:02] [info] [delight] [Models] VERIFIED FIX: Models page in REAL mode now shows 17 models from the actual LLM-Provider! Previously showed "0 models". Models visible: mistral-large (AzureInference), Claude haiku/sonnet/opus (Anthropic), phi-3-medium (AzureInference), llama-3-70b (AzureInference), gpt-4o/gpt-4 (Azure), deepseek-coder-1.3b (Local), cohere-command-r-plus (AzureInference). MODEL STATUS shows Online, device=managed. QUEUE panel populated (depth 0). PROVIDERS: Claude Code CLI + Local FastAPI. The HttpClient factory fix in Program.cs is confirmed working.
- [03:20:08] [minor] [observation] [Models] Minor issues in real mode Models page: 1) Active Model shows '-' even though LLM-Provider is online and models are available. The health endpoint may not report activeModel. 2) Backend/MaxTokens/Temperature all show '-'. 3) Metrics panel shows 'No metrics available' — this may be expected on a fresh session with no LLM calls yet. 4) 147 sessions in NavBar badge — that's a lot of accumulated test sessions.
- [03:20:17] [info] [observation] [Catalog] Catalog in real mode: 156 blocks loaded from backend! Shows diverse types: inference, ui, workflow, agent, tool, context, conversation-append. Scroll indicator shows 1/156 ▼. Type filter tabs visible. Fitness all showing '-' (no mock data, real blocks don't have fitness scores stored yet). [/] Search shortcut visible in footer. Real data loading works perfectly.
- [03:20:29] [info] [delight] [Catalog] Catalog search in REAL mode works perfectly. Searching 'maestro' filters 156 blocks down to 15 maestro-related blocks: Clear Conversation, Fitness Calculator, Maestro Agent v4, Maestro Assistant (2 variants), Maestro Assistant Workflow, Maestro CLI, Memory Read/Write, New Conversation, Project Preparer, Publisher Agent, RL Fitness Strategy, State Manager, Widget Response Formatter. Search is fast, results are relevant. Confirmed: '/' key routing fix works in real mode too.
- [03:20:47] [minor] [observation] [Home] Home page in real mode shows 'No active sessions' and no NavBar badge — the 147 sessions badge from other pages disappeared. The Home page fetches active sessions (running status) separately from all sessions. No running sessions exist currently, which is correct since no workflow is in progress.
- [03:21:00] [info] [observation] [Spaces] Spaces page in real mode: 147 sessions loaded with scroll (1/147 ▼). Shows real sessions with meaningful names, statuses (idle, created, stopped), entry points, and durations. First session auto-expanded showing full UUID and entry points (message, new-conversation, clear-conversation). The session history is extensive from all the dogfooding/testing. Tabs [1] Repos [2] Workspaces [3] Sessions all visible.
- [03:21:10] [info] [observation] [Foundry] Foundry page in real mode: 156 blocks with full type breakdown (34 workflow, 44 agent, 39 tool, 18 inference, 5 ui, etc.). The type summary line is quite long and wraps. Shows all block types including newer ones (conversation-append, conversation-read, file-edit, message-builder, response-parser, tool-dispatcher). Scroll indicator 1/156 ▼. Works correctly.
- [03:21:56] [info] [delight] [Agent] Agent page works perfectly in real mode. Message "What is 2+2? Answer in one word." → Agent responded "Four." in ~14s. Session restored from previous test (d5ae0fec — conversation persistence confirmed across TUI restarts). Full workflow visible: Read Conversation ✓ → Llm Call ✓ → Ensure Conversation ✓ → Save User Message ✓ → Load History ✓ → Execute Agent ✓ → Save Assistant Response ✓. Status bar shows "✓ Agent: completed" with response preview "Four." All three previously broken features confirmed working.
- [03:22:09] [info] [delight] [Agent] VERIFIED: '/' key on Agent page correctly focuses the input bar (shows '> Describe your task...'). On Catalog page it enters search mode. The key routing fix (App.ts line 362: input === '/' && currentPageRef.current === 'agent') works correctly — '/' is context-sensitive per page.

---
*Generated by e2e-tester agent on 2026-03-07T03:23:10.250Z*
