# Dogfooding Report — 2026-03-06

## Summary
Verification of reported fixes: 1 of 5 issues fully fixed (catalog row rendering), 1 partially fixed but broken in integration (search exists but key routing prevents input), 1 fixed in code but not verifiable (model duplicate names), 2 not fixed at all (conversation persistence, French language). The catalog truncation fix is excellent — 156 real blocks all render on single lines. But the new search feature has a critical key routing bug (App.ts intercepts '/' globally, disabling CatalogScreen input).

## Scores

| Category | Score |
|----------|-------|
| First Impression | 4/5 |
| Navigation | 4/5 |
| Core Feature (Agent) | 2/5 |
| Visual Polish | 4/5 |
| Error Handling | 3/5 |
| **Overall** | **3/5** |
| *Average* | *3.3/5* |

## Top Issues to Fix
1. STILL BROKEN — Conversation persistence: Agent gives identical response every turn, ignoring all previous context. History not reaching LLM.
2. STILL BROKEN — French language: Agent responds in French regardless of English input. System prompt / locale issue unchanged.
3. NEW BUG — Catalog search key routing: '/' in App.ts (line 362) intercepts globally, sets inputFocused=true, disabling CatalogScreen's useInput. Search bar opens but cannot receive text. Fix: only handle '/' on Agent page, or add callback for CatalogScreen to signal search mode to parent.
4. STILL BROKEN — Models page shows 0 models in real mode sidecar (cannot verify duplicate-name fix). Code fix at ModelsScreen.ts:210-212 looks correct but untestable.

## What Works Well
- FIXED: Catalog row rendering — all 156 blocks render on single lines with proper column truncation
- FIXED (code): ModelsScreen.ts:210-212 now shows 'Name (model-id)' for distinguishable model names
- NEW: [/] Search shortcut added to Catalog help bar — feature exists structurally
- Catalog type filter still works (narrowed 156 → 141 with partial 'm' search leak)
- Navigation, Agent input, contract test [T] all still working as before

## Detailed Analysis
## Verification Results — Issue by Issue

### 1. CRITICAL: Catalog Row Rendering → FIXED ✓
All 156 real blocks now render on single lines. Type badges complete, IDs properly truncated, tags capped with "...", descriptions truncated. No more wrapping across lines. A few blocks have an extra blank line below them (minor), but the column-breaking wrapping bug is gone. This was the most visible problem and it's properly solved.

### 2. CRITICAL: No Search in Catalog → PARTIALLY IMPLEMENTED, BROKEN ✗
The search feature was added — pressing '/' on Catalog shows a search bar (/█) and [/] Search is in the help bar. The code at CatalogScreen.ts:491-523 implements live filtering with useInput. HOWEVER, it doesn't work when CatalogScreen is embedded in maestro-code App because:
- App.ts:362 intercepts '/' globally (isActive: true, always on)
- App.ts sets inputFocused=true, which makes CatalogScreen receive keyboardActive=false
- CatalogScreen's useInput has isActive: keyboardActive !== false → becomes disabled
- All subsequent keystrokes go to App's input handler, not CatalogScreen's search

Evidence: typing "maestro" after '/' left the search bar showing "/█" unchanged. But when 'm' leaked through (from a page navigation attempt after stuck state), the list filtered from 156 to 141 — proving the search logic itself works, just the input routing is broken.

Fix needed: App.ts should check `currentPage !== 'agent'` before intercepting '/', or CatalogScreen should expose a callback (onSearchStart/onSearchEnd) that App uses to suppress its own '/' handler.

### 3. CRITICAL: Conversation Persistence → NOT FIXED ✗
Three attempts, three identical responses ("Salut ! 👋 Je suis l'assistant Maestro..."). The agent shows "Load History ✓" in workflow steps but clearly isn't using the history. The follow-up message explicitly referenced the previous response ("You just said 'Salut'") and the agent ignored it completely. Same session ID (17823128) reused across all attempts. This is a backend/execution bug, not a TUI bug.

### 4. MAJOR: French Language → NOT FIXED ✗
Agent consistently responds in French despite all messages being in English. This is likely in the system prompt of the maestro-assistant block or a locale detection mechanism. Not a TUI issue.

### 5. MAJOR: Duplicate Model Names → FIXED IN CODE, UNVERIFIABLE
ModelsScreen.ts:210-212 now constructs display names as "DisplayName (modelId)" when they differ. So "Claude haiku" should now show as "Claude haiku (claude-haiku-4-5)". The Models page showed "0 models available" in the sidecar's real mode so I couldn't verify visually, but the code change is correct.

## Summary Table

| Issue | Status | Severity |
|-------|--------|----------|
| Catalog row rendering | ✅ FIXED | Was Critical |
| Catalog search | ⚠️ Broken integration | Critical (NEW) |
| Conversation persistence | ❌ NOT FIXED | Critical |
| French language | ❌ NOT FIXED | Major |
| Duplicate model names | ✅ Code fixed | Was Major |

## Session Notes (raw observations)
- [17:52:34] [info] [observation] [Catalog] VERIFIED FIXED: Catalog row rendering with 156 real blocks. Almost ALL rows are now single-line. Columns are well-aligned. Type badges are complete ([inference], [workflow], [agent], [tool], [context], [conversation-append]). Tags truncated with "..." (e.g., "orchestration,llm,tra...", "analyze-code,detect-p..."). Names truncated when needed ("Autonomous Development Wo..."). IDs fit correctly. There are still 2-3 rows that have a blank line below them (Agent Improvement Loop, Block Validation Workflow, Collect Session Metrics) but the actual row content doesn't wrap. The wrapping bug is FIXED. The blank-line issue is minor and might be related to these blocks having slightly longer content that pushes to exactly the width limit.
- [17:53:00] [critical] [bug] [Catalog] Search NOT WORKING: /█ bar opened but typing "maestro" doesn't update the search text or filter the list. Still shows 156 blocks, search bar still "/█". The useInput handler at line 492 should capture characters when isSearching=true, but the typed text isn't reaching it. This is likely a focus/isActive conflict — when CatalogScreen is embedded in maestro-code App, the parent App's useInput handler might be intercepting the characters (e.g., 'm' goes to Models page navigation). The isSearching state disables CatalogScreen's useKeyboard but may not disable the parent's key handlers. This is a key routing bug between parent App and embedded CatalogScreen.
- [17:53:36] [critical] [bug] [Catalog] ROOT CAUSE FOUND for search not working: App.ts:362-364 intercepts '/' globally (isActive: true) and sets inputFocused=true. This is designed for the Agent page's input bar. When on Catalog page, '/' triggers App's handler first → sets inputFocused=true → CatalogScreen receives keyboardActive=false (line 813: !inputFocused) → CatalogScreen's useInput becomes disabled (isActive: false). The search bar shows /█ because the CatalogScreen's useState was somehow set, but all subsequent input goes to the App-level Agent input bar (which is invisible on non-Agent pages). Fix: App.ts should only handle '/' when on the Agent page, or CatalogScreen needs to communicate its search state up to App to block the '/' interception.
- [17:53:58] [critical] [bug] [Catalog] Additional search/focus bug: After '/' was pressed and inputFocused got stuck to true, pressing 'm' (intended for Models page) typed 'm' into the search instead. The search bar now shows "/m█" and list filtered from 156 to 141 blocks — so the search IS partially working (it filtered out 15 blocks not matching 'm')! But the state is inconsistent: Escape didn't clear the search earlier, and now the user is trapped in search mode with no way to navigate away. The inputFocused=true in App.ts persists because the CatalogScreen can't tell App to unfocus. This is a state management bug across components.
- [17:54:35] [info] [observation] [Models] Models page shows "0 models available" despite LLM-Provider running on 5010 with 17 models. The sidecar's API routing may not be using the same endpoint. Cannot verify the duplicate-names fix visually. However, code review confirms the fix at ModelsScreen.ts:210-212: displayName now shows "Name (model-id)" when they differ (e.g., "Claude haiku (claude-haiku-4-5)"). The code fix is CORRECT but cannot be verified end-to-end in this session.
- [17:55:27] [critical] [bug] [Agent] NOT FIXED: Conversation persistence still broken. Asked "You just said 'Salut'. Can you repeat what you said but in English?" — got the EXACT SAME response for the third time: "Salut ! 👋 Je suis l'assistant Maestro..." The agent has zero memory of previous turns. Also NOT FIXED: French language persists despite English-only input. These are the same two bugs from the original dogfooding report. Both are still present.

---
*Generated by e2e-tester agent on 2026-03-06T17:56:09.009Z*
