/** Layout constants for the TUI */
export const layout = {
  headerHeight: 3,
  statusBarHeight: 3,
  borderWidth: 1,
  minWidth: 80,
  minHeight: 24,
  panelPadding: 1,
  refreshIntervalMs: 2000,
} as const;
