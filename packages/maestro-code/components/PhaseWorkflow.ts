/**
 * PhaseWorkflow — Re-exported from @maestro/tui.
 * The canonical implementation lives in @maestro/tui/components.
 */
export { PhaseWorkflow } from '@maestro/tui/components';
// Re-export tree utility for backward compat
export { flattenPhaseWorkflow } from '@maestro/tui/utils';
