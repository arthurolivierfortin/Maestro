/**
 * WorkflowTree — Re-exported from @maestro/tui.
 * The canonical implementation lives in @maestro/tui/components.
 */
export { WorkflowTree } from '@maestro/tui/components';
// Re-export tree utilities for backward compat
export { flattenExecutionTree, autoExpandRunningPath } from '@maestro/tui/utils';
