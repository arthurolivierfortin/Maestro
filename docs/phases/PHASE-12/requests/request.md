maintenant que les sessions peuvent être basé sur un repo, j'aimerais que vous setupiez deux sessions sur deux repos différents. Dans la dernière    
  phase, une session consitent à utiliser un modèle pour créer un worflow qui réussira à faire un commit description. Faites une analyse de cette       
  session. Cette session a montré que parfois, le inference model répondait avec un résultat non structuré. J'aimerais donc que vous fassiez une autre  
  session dans le même genre, qui ell aura comme but de tester tout les modèles locaux que llm provider montre comme disponible, en testant si ils      
  sont capable de respecter une structure en faisant plusieurs teste différents. Dans cette session, nous allons utiliser une logique similaire à       
  l'autre en commençant par tester directement des inference blocks en changeant les modèles en suivant toujours une philosophie maestro. Cette         
  session doit comporter plusieurs phases qui seront déterminé par vous pour tester. Il doit aussi contenir une phase de documentation comme la         
  session de la dernière phase, d'où vient l'importance que cette session soit sur un repo pour que je puisse accéder à la doc.                         
  Pour ces tâches il sera important aussi de valider et solidifier au besoin le workflow actuel qui est le worflow attendu actuellement. Vous êtes      
  l'authorité, vous créez une session par le cli, vous utilisez le cli pour setuper la session. Lorsque la session et prête, vous lancez le monitor     
  pour que je puisse voir ce que vous faites. Ensuite vous lancez le workflow principale jusqu'à temps qu'il soit complété. Si jamais il nécessite des  
  correction, faites les jusqu'à temps que la session est bien fonctionné. Je devrais à la fin pouvoir aller dans le repo et voir la documentation      
  que le workflow a généré et non vous.  