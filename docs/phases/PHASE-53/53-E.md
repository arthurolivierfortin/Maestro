# 53-E : Protection anti-regression + documentation

**Effort** : 0.5 jour
**Prerequis** : 53-D COMPLETE

---

## 1. Commentaires dans le code

### Dans `AgentBlockExecutor.cs`

```csharp
// === ARCHITECTURE RULE (ADR-AGENT-AS-WORKFLOW, 2026-03-05) ===
// AgentBlockExecutor MUST NOT contain tool dispatch, response parsing,
// or agentic loop logic. These are defined as config.nodes in the
// agent's block.json and executed by the base class (MultiNodeBlockExecutor).
//
// AgentBlockExecutor only handles:
// - Conversation creation (PrepareExecutionAsync)
// - I/O contract: prompt/messages in → text out (ExtractResultAsync)
//
// If you need to change agent behavior, modify the config.nodes in
// the block.json or the node templates, NOT this executor.
// See: docs/phases/PHASE-53/ADR-AGENT-AS-WORKFLOW.md
// ============================================================
```

### Dans `ToolBlockExecutor.cs`

```csharp
// === ARCHITECTURE RULE (ADR-AGENT-AS-WORKFLOW, 2026-03-05) ===
// ToolBlockExecutor MUST NOT contain if-chains for different tool types.
// Each tool has its own config.nodes in its block.json.
// ToolBlockExecutor only handles:
// - Input mapping from block.json schema (PrepareExecutionAsync)
// - Output mapping from block.json schema (ExtractResultAsync)
//
// If you need a new tool type, create a new block.json with config.nodes.
// NEVER add "if (toolType == 'xxx')" here.
// See: docs/phases/PHASE-53/ADR-AGENT-AS-WORKFLOW.md
// ============================================================
```

### Dans `NodeExecutionEngine.cs`

```csharp
// === ARCHITECTURE RULE (2026-03-05) ===
// NodeExecutionEngine is PURE CONTROL FLOW.
// It executes while, conditional, sequence, parallel, for-each, set-variable.
// When it encounters a blockRef, it dispatches via BlockExecutorRegistry.
// It NEVER executes logic itself — it orchestrates.
// No LLM calls, no file I/O, no shell commands here.
// ============================================================
```

---

## 2. Mise a jour CLAUDE.md

Deja fait dans cette phase (voir les modifications precedentes). Verifier que les pitfalls suivants sont presents :
- "NEVER add tool dispatch in AgentBlockExecutor"
- "NEVER add handlers inline in ToolBlockExecutor"
- Reference a l'ADR Phase 53

---

## 3. Mise a jour docs architecture

- `docs/system/architecture/execution.md` : section "Multi-Node Blocks" (agent + workflow + tool share base class)
- `docs/system/architecture/blocks.md` : clarifier que `blockType: "agent"` = multi-node block avec I/O inference
- `docs/system/architecture/blocks.md` : clarifier que `blockType: "tool"` = multi-node block avec I/O schema

---

## 4. Mise a jour memory

- `memory/MEMORY.md` : verifier que la section architecture est a jour
- `memory/architecture.md` : verifier que la hierarchie complete est documentee

---

## 5. Supprimer le code mort

- L'ancien `AgentBlockExecutor` monolithique (si encore present)
- L'ancien `ToolBlockExecutor` avec if-chains (si encore present)
- Les handlers natifs inline dans l'ancien `EntryPointExecutor` (devenus des blocks)
- Tout code de fallback/compat qui n'est plus necessaire

**REGLE CLAUDE.md** : "No legacy support. When a system is replaced, remove the old code entirely."
