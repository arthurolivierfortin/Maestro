# Phase 58 — Analyse de situation (2026-03-14)

**Agent** : Claude Opus 4.6
**Dernier commit** : e9cce88 (2026-03-08) — 6 jours d'inactivite
**Branch** : main

---

## Etat des lieux

### Ce qui est FAIT et solide

| Composant | Phase | Status |
|-----------|-------|--------|
| NodeExecutionEngine (786L, 3 INodeHandler) | 53 | STABLE |
| BlockExecutorRegistry (un registre, un executor/type) | 53 | STABLE |
| Pipeline de couts reels (IModelPricingService) | 56 | STABLE |
| Systeme de contrats (6 contrats, FitnessScore) | 51-54 | STABLE |
| Agent-creator (994L system prompt, boucle create-test-fix) | 57 | STABLE |
| TUI maestro-code (6 pages, 36 composants, 154/156 tests) | 45-56 | STABLE |
| block-forge workflow (7 nodes) + request-analyzer | 58-A | DONE |
| `/create-agent` TUI + CLI + 8 tests | 58-B | DONE |

### Ce qui est BLOQUE

**58-C Dogfooding** — `/create-agent` retourne **404 "Not Found"** a l'invocation backend.

- L'UI fonctionne (parsing, progression)
- Le backend refuse l'invocation du workflow block-forge
- Cause non diagnostiquee depuis 6 jours

### Ce qui reste ouvert d'avant

**F2 (Phase 56-F)** — Conversation persistence non verifiee en E2E. Le 2e message recoit la meme reponse que le 1er. Investigation necessaire.

### Issues cosmetiques (rapport dogfooding 2026-03-08)

1. BlockDetail Type: `[unknown]` — blockType pas passe au composant detail
2. ModelsScreen Providers: `-` — mapping DTO incomplet
3. Catalog search Escape — efface le filtre au lieu de le garder
4. BlockDetail INFO — champs capabilities/contract/tags manquants

---

## Metriques

| Metrique | Valeur |
|----------|--------|
| Phases completees | 57 / 64 |
| Tests passants | ~350+ |
| Blocs catalogue | 159 |
| Contrats | 6 |
| Modeles supportes | 17 |
| Score dogfooding TUI | 4.0/5 |
| NodeExecutionEngine | 786L (baisse 81.6%) |
| Jours depuis dernier commit | 6 |

---

## Plan d'action immediat

### 1. Debloquer le 404 (priorite critique)

**Diagnostic** :
1. Demarrer les services (backend 5000, LLM-Provider 5010)
2. Verifier decouverte : `curl http://localhost:5000/api/blocks?search=block-forge`
3. Verifier le template : `curl http://localhost:5000/api/sessions/templates`
4. Tester creation manuelle de session block-forge via CLI
5. Tester invocation via curl directement
6. Lire les logs backend pour le message d'erreur exact

**Causes probables** :
- Entry points dans `block-forge.session.json` mal configures
- Workflow pas decouvert par `FileSystemBlockDiscoveryService`
- Path de session ou d'invocation incorrect dans le code `/create-agent`

### 2. Completer 58-C (dogfooding)

**Criteres** :
- 2 agents crees via `/create-agent` avec fitness > 0.5
- Process plus rapide que creation manuelle
- Score >= 3/5

### 3. Resoudre F2 conversation persistence

**Investigation** :
- Verifier que `conversation-read` charge les messages precedents
- Verifier que le prompt envoye au LLM contient l'historique
- Tester avec 2 messages successifs via curl

---

## Risques

1. Le 404 pourrait etre un probleme profond (decouverte de blocks, routing des entry points)
2. F2 pourrait reveler un bug architectural dans la persistence de conversation
3. Si ces deux bloqueurs prennent > 2 jours, la phase 58 depasse son budget de 3 jours max
