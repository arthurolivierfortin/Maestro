/**
 * useApiData — Re-export from shared/app/hooks/usePolling.
 *
 * This is the legacy name. The canonical hook is usePolling in shared/app/hooks.
 */

export { usePolling as useApiData, type ConnectionStatus, type UsePollingReturn as UseApiDataReturn } from '../../app/hooks/usePolling.ts';
